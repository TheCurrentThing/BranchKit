// Stub — products are not yet in the payload pipeline.
// Renders nothing until the products table is wired into SitePayload.
// The section slot is reserved so StandardRenderer dispatch is correct.
export function FeaturedProductsSection() {
  return null;
}
